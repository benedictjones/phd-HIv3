network:

  # # Define the properties of the defualt material neuron
  model : Physical # Physical, Pks, LDs
  num_input : 2
  num_output : 3
  num_config : 1
  num_nodes : default # 18 # default  # 'default' = sum of above, or int


# # Not actually SPICE, but defines system netowrk
spice:
  Vmin : -9.5  # -5
  Vmax : 9.5  # 5

  op_shunt_R : 470

  in_pins : [1]
  out_pins : [4]
  num_samples : 30
 # fin
